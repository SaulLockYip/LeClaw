---
phase: 01-foundation-cli-init
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/src/index.ts
  - packages/cli/src/program/build-program.ts
  - packages/cli/src/program/configure-help.ts
  - packages/cli/src/commands/init-command.ts
  - packages/cli/src/commands/config-command.ts
  - packages/cli/src/commands/config-openclaw-command.ts
  - packages/cli/src/commands/config-gateway-command.ts
  - packages/cli/src/commands/status-command.ts
  - packages/cli/src/commands/start-command.ts
  - packages/cli/src/run-cli.ts
  - packages/cli/bin/leclaw.js
  - packages/cli/vitest.config.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/server/src/app.ts
  - packages/server/src/routes/health.ts
  - packages/server/vitest.config.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05

must_haves:
  truths:
    - "CLI parses Commander.js subcommands and outputs structured JSON"
    - "leclaw init creates ~/.leclaw/ directory"
    - "leclaw config openclaw --dir <path> stores OpenClaw directory in config"
    - "leclaw config gateway --url <url> --key <key> stores Gateway settings in config"
    - "Server package exposes /health endpoint"
  artifacts:
    - path: "packages/cli/src/program/build-program.ts"
      provides: "Commander.js program with subcommand registration"
      exports: ["buildProgram", "runCli"]
    - path: "packages/cli/src/commands/config-command.ts"
      provides: "Config subcommands (get, set, openclaw, gateway)"
    - path: "packages/server/src/app.ts"
      provides: "Express app with /health endpoint"
      exports: ["createApp"]
  key_links:
    - from: "packages/cli"
      to: "packages/shared"
      via: "import { LeClawConfig, loadConfig, writeConfig }"
      pattern: "from \"@leclaw/shared\""
    - from: "packages/server"
      to: "packages/db"
      via: "import { initializeDb }"
      pattern: "from \"@leclaw/db\""
---

<objective>
Create packages/cli with Commander.js-based CLI commands and packages/server with Express backend skeleton and /health endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-cli-init/01-CONTEXT.md
@.planning/phases/01-foundation-cli-init/RESEARCH.md

# Key interfaces this plan USES (not creates):
From packages/shared (created in 01-01-PLAN.md):
```typescript
export interface LeClawConfig {
  version: string;
  openclaw: { dir: string; };
  gateway: { url: string; key: string; };
  database: { connectionString: string; };
}
export function loadConfig(configPath: string): LeClawConfig;
export function writeConfig(configPath: string, config: LeClawConfig): Promise<void>;
```

From packages/db (created in 01-01-PLAN.md):
```typescript
export function initializeDb(config?: DbConfig): Promise<{
  connectionString: string;
  cleanup: () => Promise<void>;
}>;
```

# Decisions from 01-CONTEXT.md:
- D-01: CLI output = Structured JSON (machine-parseable)
- D-02: Config file = JSON (~/.leclaw/config.json)
- D-03: Config directory = ~/.leclaw/
- D-04: Errors via verbose stderr
- D-05: Exit codes: 0 = success, non-zero = error
- D-06: leclaw init = pure non-interactive
- D-07: NO @clack/prompts
- D-08: camelCase naming
- D-20: Husky git hooks
</context>

<tasks>

<task type="auto">
  <name>Task 1: packages/cli with Commander.js structure</name>
  <files>packages/cli/package.json, packages/cli/tsconfig.json, packages/cli/src/index.ts, packages/cli/src/program/build-program.ts, packages/cli/src/program/configure-help.ts, packages/cli/src/commands/init-command.ts, packages/cli/src/commands/config-command.ts, packages/cli/src/commands/config-openclaw-command.ts, packages/cli/src/commands/config-gateway-command.ts, packages/cli/src/commands/status-command.ts, packages/cli/src/commands/start-command.ts, packages/cli/src/run-cli.ts, packages/cli/bin/leclaw.js, packages/cli/vitest.config.ts</files>
  <action>
    Create packages/cli with Commander.js-based CLI using subcommand pattern.

    **packages/cli/package.json:**
    - name: "@leclaw/cli"
    - type: "module"
    - bin: { "leclaw": "./bin/leclaw.js" }
    - dependencies: { commander: "^12.0.0", "@leclaw/shared": "workspace:*", "@leclaw/db": "workspace:*" }
    - devDependencies: { vitest }

    **packages/cli/tsconfig.json:**
    - extends: "../../tsconfig.json"
    - outDir: "./dist"

    **packages/cli/bin/leclaw.js:**
    ```javascript
    #!/usr/bin/env node
    import { runCli } from "../dist/index.js";
    runCli(process.argv).catch((err) => {
      console.error(JSON.stringify({ error: err.message, code: "UNKNOWN_ERROR" }));
      process.exit(1);
    });
    ```

    **packages/cli/src/run-cli.ts:**
    ```typescript
    export async function runCli(argv: string[] = process.argv): Promise<void> {
      const program = buildProgram();
      await program.parseAsync(argv);
    }
    ```

    **packages/cli/src/program/build-program.ts:**
    ```typescript
    import { Command } from "commander";
    import { configureProgramHelp } from "./configure-help.js";

    export function buildProgram(): Command {
      const program = new Command();
      program.name("leclaw").description("LeClaw - Agent Management Platform").version("1.0.0");

      configureProgramHelp(program);
      registerCommands(program);

      return program;
    }

    function registerCommands(program: Command): void {
      // Import and register each command
      // leclaw init
      // leclaw config
      // leclaw status
      // leclaw start
    }
    ```

    **packages/cli/src/program/configure-help.ts:**
    - Configure --help to output JSON format when requested
    - Custom help text formatting

    **packages/cli/src/commands/init-command.ts:**
    ```typescript
    export function registerInitCommand(program: Command): void {
      program
        .command("init")
        .description("Initialize LeClaw configuration directory (~/.leclaw/)")
        .option("--dir <path>", "Custom config directory path")
        .action(async (opts) => {
          // Output: { success: true, configDir: "~/.leclaw/" }
          // Creates ~/.leclaw/ directory if not exists
          // Creates ~/.leclaw/config.json with defaults
        });
    }
    ```

    **packages/cli/src/commands/config-command.ts:**
    ```typescript
    // leclaw config - displays current config
    // leclaw config openclaw --dir <path>
    // leclaw config gateway --url <url> --key <key>
    export function registerConfigCommand(program: Command): void {
      const config = program.command("config").description("Manage LeClaw configuration");

      // leclaw config (show current)
      config.action(async () => {
        const configPath = path.join(os.homedir(), ".leclaw", "config.json");
        const cfg = loadConfig(configPath);
        console.log(JSON.stringify({ success: true, config: cfg }, null, 2));
      });

      // Register openclaw and gateway subcommands
      registerConfigOpenClawCommand(config);
      registerConfigGatewayCommand(config);
    }
    ```

    **packages/cli/src/commands/config-openclaw-command.ts:**
    ```typescript
    export function registerConfigOpenClawCommand(program: Command): void {
      program
        .command("openclaw")
        .description("Configure OpenClaw directory")
        .requiredOption("--dir <path>", "OpenClaw directory path")
        .action(async (opts) => {
          // Read existing config
          // Update openclaw.dir
          // Write config atomically
          // Output: { success: true, openclaw: { dir: <path> } }
        });
    }
    ```

    **packages/cli/src/commands/config-gateway-command.ts:**
    ```typescript
    export function registerConfigGatewayCommand(program: Command): void {
      program
        .command("gateway")
        .description("Configure Gateway URL and API key")
        .requiredOption("--url <url>", "Gateway WebSocket URL")
        .requiredOption("--key <key>", "Gateway API key")
        .action(async (opts) => {
          // Read existing config
          // Update gateway.url and gateway.key
          // Write config atomically
          // Output: { success: true, gateway: { url, key: "***" } }
        });
    }
    ```

    **packages/cli/src/commands/status-command.ts:**
    ```typescript
    // leclaw status - Placeholder that checks config and reports status
    // Full implementation in Plan 03
    export function registerStatusCommand(program: Command): void {
      program
        .command("status")
        .description("View connection status to OpenClaw and Gateway")
        .action(async () => {
          // Read config
          // Output: { success: true, status: { openclaw: {...}, gateway: {...} } }
          // Full connectivity checks in Plan 03
        });
    }
    ```

    **packages/cli/src/commands/start-command.ts:**
    ```typescript
    // leclaw start - Placeholder that imports server package
    // Full implementation in Plan 03
    export function registerStartCommand(program: Command): void {
      program
        .command("start")
        .description("Start LeClaw server (backend + web UI)")
        .option("--port <port>", "Server port", "3000")
        .action(async (opts) => {
          // Import and start server from @leclaw/server
          // Output: { success: true, port: <port> }
          // Full implementation in Plan 03
        });
    }
    ```

    **packages/cli/src/index.ts:**
    ```typescript
    export { runCli } from "./run-cli.js";
    export { buildProgram } from "./program/build-program.js";
    ```

    **packages/cli/vitest.config.ts:**
    - extends: "../../vitest.shared.config.ts"
    - include: ["src/**/*.test.ts"]

    **Output format for ALL commands (per D-01):**
    ```json
    { "success": true, ...data }
    { "success": false, "error": "message", "code": "ERROR_CODE" }
    ```

    **Exit codes (per D-05):** 0 = success, 1+ = error
  </action>
  <verify>
    <automated>cd packages/cli && pnpm build && node dist/bin/leclaw.js --help</automated>
  </verify>
  <done>
    CLI parses Commander.js subcommands. `leclaw --help` outputs structured help. Commands output JSON. Config commands read/write to ~/.leclaw/config.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: packages/server with Express backend skeleton</name>
  <files>packages/server/package.json, packages/server/tsconfig.json, packages/server/src/index.ts, packages/server/src/app.ts, packages/server/src/routes/health.ts, packages/server/vitest.config.ts</files>
  <action>
    Create packages/server with Express backend skeleton and /health endpoint.

    **packages/server/package.json:**
    - name: "@leclaw/server"
    - type: "module"
    - main: "./dist/index.js"
    - dependencies: { express: "^4.18.0", cors: "^2.8.5", "@leclaw/shared": "workspace:*", "@leclaw/db": "workspace:*" }
    - devDependencies: { vitest, "@types/express": "^4.17.0", "@types/cors": "^2.8.0" }

    **packages/server/tsconfig.json:**
    - extends: "../../tsconfig.json"
    - outDir: "./dist"

    **packages/server/src/app.ts:**
    ```typescript
    import express, { Express } from "express";
    import cors from "cors";
    import { healthRouter } from "./routes/health.js";

    export function createApp(): Express {
      const app = express();
      app.use(cors());
      app.use(express.json());
      app.use("/health", healthRouter);
      return app;
    }
    ```

    **packages/server/src/routes/health.ts:**
    ```typescript
    import { Router, Request, Response } from "express";

    export const healthRouter = Router();

    healthRouter.get("/", (req: Request, res: Response) => {
      res.json({
        status: "ok",
        timestamp: new Date().toISOString(),
        version: "1.0.0",
      });
    });
    ```

    **packages/server/src/index.ts:**
    ```typescript
    export { createApp } from "./app.js";
    import { createApp } from "./app.js";

    const PORT = parseInt(process.env.PORT ?? "3000", 10);
    const app = createApp();

    app.listen(PORT, () => {
      console.log(JSON.stringify({ success: true, server: { port: PORT } }));
    });
    ```

    **packages/server/vitest.config.ts:**
    - extends: "../../vitest.shared.config.ts"
    - include: ["src/**/*.test.ts"]

    **Write tests in src/app.test.ts:**
    - Test createApp() returns Express instance
    - Test /health endpoint returns { status: "ok" }
  </action>
  <verify>
    <automated>cd packages/server && pnpm build && pnpm test -- --run</automated>
  </verify>
  <done>
    Server package builds successfully. Express app created with /health endpoint. Tests pass.
  </done>
</task>

</tasks>

<verification>
- packages/cli/bin/leclaw.js is executable
- `node bin/leclaw.js --help` outputs help text
- `node bin/leclaw.js init` creates ~/.leclaw/config.json
- `node bin/leclaw.js config` outputs current config as JSON
- `node bin/leclaw.js config openclaw --dir /tmp/test` updates config
- `node bin/leclaw.js config gateway --url ws://example.com --key test123` updates config
- Server /health endpoint returns { status: "ok" }
</verification>

<success_criteria>
1. CLI parses Commander.js subcommands
2. leclaw init creates ~/.leclaw/ directory with config.json
3. leclaw config openclaw --dir stores path in config
4. leclaw config gateway --url --key stores settings in config
5. Server package has working /health endpoint
6. All code uses camelCase naming (per D-08)
7. All commands output structured JSON (per D-01)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli-init/01-02-SUMMARY.md`
</output>
